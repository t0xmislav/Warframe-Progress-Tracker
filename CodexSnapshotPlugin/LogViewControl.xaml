<UserControl x:Class="CodexSnapshotPlugin.LogViewControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CodexSnapshotPlugin"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <!-- Provide the converter resource referenced by the DataGrid template -->
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <Grid>
        <DataGrid x:Name="LogDataGrid" AutoGenerateColumns="False" ItemsSource="{Binding Logs}" IsReadOnly="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Timestamp" Binding="{Binding Path=Timestamp, StringFormat='{}{0:G}'}" IsReadOnly="True" Width="200"/>
                <DataGridTextColumn Header="Action" Binding="{Binding Path=Action}" IsReadOnly="True" Width="*"/>
                <DataGridTextColumn Header="Details" Binding="{Binding Path=Details}" IsReadOnly="True" Width="2*"/>
                <DataGridTemplateColumn Header="Admin Note" Width="2*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding AdminNote}" TextWrapping="Wrap" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                    <DataGridTemplateColumn.CellEditingTemplate>
                        <DataTemplate>
                            <TextBox Text="{Binding AdminNote, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                             IsEnabled="{Binding DataContext.IsAdmin, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                             KeyDown="AdminNoteTextBox_KeyDown"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellEditingTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Header="" Width="70">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Save" Command="{Binding SaveCommand}" 
                            IsEnabled="{Binding IsDirty}"
                            Visibility="{Binding DataContext.IsAdmin, RelativeSource={RelativeSource AncestorType=DataGrid}, Converter={StaticResource BoolToVis}}"
                            Width="50"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Last Modified" Binding="{Binding Path=LastModified, StringFormat='{}{0:G}'}" IsReadOnly="True" Width="200"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
