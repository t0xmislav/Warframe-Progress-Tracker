<Window x:Class="Warframe_Progress_Tracker.View.AccountSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Warframe_Progress_Tracker"
        mc:Ignorable="d"
        Background="{DynamicResource PrimaryBackgroundBrush}"
        Foreground="{DynamicResource PrimaryForegroundBrush}"
        Title="Account settings" Height="300" Width="450"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="{DynamicResource AccountDisplayNameStr}" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox x:Name="DisplayNameBox" Grid.Row="0" Grid.Column="1" Width="200"/>

        <TextBlock Text="{DynamicResource AccountPlatformStr}" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center"/>
        <ComboBox x:Name="PlatformBox" Grid.Row="1" Grid.Column="1" Width="120">
            <ComboBoxItem Content="pc" IsSelected="True"/>
            <ComboBoxItem Content="ps4"/>
            <ComboBoxItem Content="xbox"/>
            <ComboBoxItem Content="switch"/>
        </ComboBox>
        <TextBlock Text="{DynamicResource LanguageStr}" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center"/>
        <ComboBox x:Name="LanguageBox" Grid.Row="2" Grid.Column="1" Width="120">
            <ComboBoxItem Content="English" Tag="en"/>
            <ComboBoxItem Content="German" Tag="de"/>
            
        </ComboBox>
        <TextBlock Text="{DynamicResource ThemeStr}" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center"/>
        <ComboBox x:Name="ThemeBox" Grid.Row="3" Grid.Column="1" Width="120">
            <ComboBoxItem Content="Light" Tag="Light"/>
            <ComboBoxItem Content="Dark" Tag="Dark"/>

        </ComboBox>
        <TextBlock Text="{DynamicResource SpeedStr}" Grid.Row="4" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox x:Name="SpeedLimitTextBox" Grid.Row="4" Grid.Column="1" Width="120"/>
        <StackPanel Grid.Row="5" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
            <Button Content="{DynamicResource SaveStr}" Width="100" Margin="5" Click="Save_Click"/>
            <Button Content="{DynamicResource AccountCancelStr}" Width="100" Margin="5" Click="Cancel_Click"/>
            <Button Content="{DynamicResource DeleteAccountStr}" Width="120" Margin="5" Click="DeleteAccount_Click"/>
        </StackPanel>
    </Grid>
</Window>
