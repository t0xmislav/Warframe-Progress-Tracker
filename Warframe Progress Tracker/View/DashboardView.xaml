<UserControl x:Class="Warframe_Progress_Tracker.View.DashboardView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Warframe_Progress_Tracker.View"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      >
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <DockPanel Margin="20">
            <StackPanel DockPanel.Dock="Top" Margin="0,0,0,20">
                <TextBlock Text="{DynamicResource DashboardTotalStr}" FontSize="16" FontWeight="Bold"/>
                <TextBlock Text="{Binding TotalRankText, Mode=OneWay}" FontSize="14" Margin="0,5,0,0"/>
                <ProgressBar Value="{Binding TotalProgressPercentage, Mode=OneWay}" Height="20" Margin="0,5,0,20" Maximum="1"/>
                <TextBlock Text="{Binding TotalProgressPercentageText, Mode=OneWay}" HorizontalAlignment="Right" FontStyle="Italic"/>
            </StackPanel>
            <StackPanel>
                <ItemsControl ItemsSource="{Binding CategoryProgresses}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="2"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="#333" BorderThickness="1" CornerRadius="8" Padding="10" Margin="8">
                                <StackPanel>
                                    <TextBlock x:Name="CategoryTxt" Text="{Binding Category.DisplayName}" FontWeight="Bold" FontSize="14"/>
                                    <ProgressBar Value="{Binding Progress, Mode=OneWay}" Maximum="100" Height="20" Margin="0,5,0,0"/>
                                    <TextBlock Text="{Binding DisplayText}" HorizontalAlignment="Right" FontStyle="Italic" FontSize="12"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </DockPanel>
    </ScrollViewer>

</UserControl>
