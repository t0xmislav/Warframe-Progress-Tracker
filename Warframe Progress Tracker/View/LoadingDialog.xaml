<Window x:Class="Warframe_Progress_Tracker.View.LoadingDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Warframe_Progress_Tracker.View"
        mc:Ignorable="d"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource PrimaryBackgroundBrush}"
        Foreground="{DynamicResource PrimaryForegroundBrush}"
        ResizeMode="NoResize"
        Title="Loading" Height="170" Width="350"
        ShowInTaskbar="False" Topmost="True">
    <Border Padding="5">
        <Grid RenderTransformOrigin="0.5,0.5">
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition Height="40"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <ProgressBar x:Name="MainProgressBar" IsIndeterminate="True" Width="200" Height="20" Margin="0,0,0,10" Grid.Row="0" Minimum="0" Maximum="1"/>
            <TextBlock x:Name="MessageBlock" Text="{DynamicResource LoadingTextStr}" FontSize="14" HorizontalAlignment="Center" Grid.Row="1" VerticalAlignment="Center"/>
            <Button x:Name="CancelButton" Content="Cancel" Click="CancelButton_Click" Width="70" Grid.Row="2" HorizontalAlignment="Right" VerticalAlignment="Center"/>
            
        </Grid>
        
    </Border>
</Window>
