<UserControl x:Class="Warframe_Progress_Tracker.View.NodeCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Warframe_Progress_Tracker.View"
             mc:Ignorable="d" 
             Loaded="UserControl_Loaded"
             d:DesignHeight="200" d:DesignWidth="150">
    <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="5" Background="Black">
        <Grid>
            <Grid.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Edit" Click="EditNode_Click"/>
                    <MenuItem Header="Delete" Click="DeleteNode_Click"/>
                </ContextMenu>
            </Grid.ContextMenu>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Image Source="{Binding ImageBitmap}" Stretch="UniformToFill"/>
            <StackPanel Grid.Row="1" Background="#AA000000" Margin="5">
                <TextBlock Text="{Binding Name}" Foreground="White" TextWrapping="Wrap" FontWeight="Bold" FontSize="12" HorizontalAlignment="Center"/>
                <CheckBox x:Name="ClearedNormalCheck" Content="Normal Clear" Foreground="White" Margin="0,5,0,0">
                </CheckBox>
                <TextBlock x:Name="NormalDateBlock" Foreground="LightGray" FontSize="10" TextAlignment="Center">
                </TextBlock>
                <CheckBox x:Name="ClearedSteelCheck" Content="Steel Path Clear" Foreground="White">
                </CheckBox>
                <TextBlock x:Name="SteelDateBlock" Foreground="LightGray" FontSize="10" TextAlignment="Center">
                </TextBlock>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
